# Copia este archivo a .env y personaliza las variables
SECRET_KEY=change-me
# Usa un path dentro del contenedor; compose monta /app/data
DATABASE_URL=sqlite:////app/data/crm.db
# FLASK_ENV=production
